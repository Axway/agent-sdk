#############################################################
# Section for external includes
############################################################
include:
  - project: "apigov/agents/beano_cicd"
    ref: APIGOV-33492
    # this one MUST be after the security jobs in order for overrides to work correctly!
    file:
      - "/gitlabci/repos/agent-sdk.yml"
